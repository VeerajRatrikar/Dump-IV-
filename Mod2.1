1.Sorting Comparator
2.Patter-suntex-slicer
3.Java SHA-256
###Java is platform independent
sample.java
class sample{
  public static void main(String[] args){
  System.out.println("VVCE");
  }
}


exe:javac sample.java
   |
   |
   ▼
bytecode : sample.class
static->> no instance is required i.e. objects arent required.

class Checker implements Comparator<Player> {
    
    @Override
    public int compare(Player player1, Player player2) {
        // First, compare scores in descending order
        if (player1.score != player2.score) {
            return player2.score - player1.score;
        }
        // If scores are the same, compare names in ascending order
        return player1.name.compareTo(player2.name);
    }
}
